// Placeholder test. The full app requires AppConfig.setup() and a configured
// Firebase app before it can be pumped, so per-app smoke tests live in each
// generated project rather than the template.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('template smoke test', () {
    expect(true, isTrue);
  });
}
